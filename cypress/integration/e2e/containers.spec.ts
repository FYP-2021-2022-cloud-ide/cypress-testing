describe("container test", () => {
  before(() => {});
  beforeEach(() => {});
  afterEach(() => {});
  after(() => {});
  it("create 3 containers", () => {});

  it("fail to create the fourth container", () => {});

  it("remove all containers", () => {});

  it("create a custom environment and revisit the dashboard before the container is created", () => {});
});

export {};
