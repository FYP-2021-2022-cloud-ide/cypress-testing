/// <reference types="cypress"/>

describe("instructor see student workspace", () => {
    before(() => {

    })

    beforeEach(() => {

    })

    it("check empty if template is published", () => {

    })

    it("check empty if course has student", () => {

    })

    it("check table if template is published and course has student", () => {

    })
})