/// <reference types="cypress" />

describe("instructor templates test", () => {

    before(() => {

    })
    beforeEach(() => {

    })
    it("create a template (when no environment)", () => {

    })

    it("create a template", () => {

    })

    it("update a template", () => {

    })

    it("publish a template", () => {
        // the UI should change 
        // toast should show up
        // should broadcast message
    })

    it("unpublish a template", () => {

    })

    it("start a template workspace", () => {

    })

    it("stop a template workspace", () => {

    })

    it("remove an environment (when some templates still using it)", () => {

    })

    it("highlight templates of an environment", () => {

    })

    it("remove a template", () => {

    })

    it("cannot rmeove a template when the workspace is started", () => {

    })

})