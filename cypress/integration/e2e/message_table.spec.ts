describe("messages table test", () => {
  before(() => {});
  beforeEach(() => {});

  it("empty if no message", () => {
    // message table have empty UI
    // alarm menu should have empty UI
  });

  it("send messages if 'send test message' button exists", () => {
    // have toast
    // message number bump
    // message table should update
  });

  it("click a message to view content", () => {});

  it("click a message on alarm menut to go to message", () => {});

  it("click more button of alarm menu to go message table", () => {});

  it("reply a message", () => {});

  it("delete a message", () => {});

  it("delete all messages", () => {});
});

export {};
